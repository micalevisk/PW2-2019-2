#!/usr/bin/env node

/**
 * Load environment variables.
 */
require('dotenv-safe').config({
  allowEmptyValues: false,
  example: process.env.NODE_ENV === 'production' ? '.env.prod.example' : '.env.example',
  path: process.env.NODE_ENV === 'development' ? '.env.dev' : '.env'
});

